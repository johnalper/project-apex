{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".build/**", ".next/**", "dist/**", ".shopify/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
